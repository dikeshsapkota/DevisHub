# Leave VITE_API_BASE_URL unset on Vercel when the API is deployed with this app.
# VITE_API_BASE_URL=http://localhost:5000/api

# Optional. Set this only when a long-running Socket.IO backend is deployed separately.
# VITE_SOCKET_URL=http://localhost:5000
